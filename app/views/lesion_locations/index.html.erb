<h1>Listing lesion_locations</h1>

<table>
  <tr>
    <th>Region</th>
    <th>Location</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @lesion_locations.each do |lesion_location| %>
  <tr>
    <td><%= lesion_location.region %></td>
    <td><%= lesion_location.location %></td>
    <td><%= link_to 'Show', lesion_location %></td>
    <td><%= link_to 'Edit', edit_lesion_location_path(lesion_location) %></td>
    <td><%= link_to 'Destroy', lesion_location, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Lesion location', new_lesion_location_path %>
